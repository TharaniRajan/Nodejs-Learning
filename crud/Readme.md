Nodejs Sample CRUD Application with MongoDb

To run Nodejs server:

install dependencies:
>npm install

To start server:
>node server.js

To run Angular4 client:

>npm install 

To start client:
>npm start


To Run The Test Cases for client:
>ng test

